@model HomeViewModel

@await Html.PartialAsync("_Carousel")

<h2>Os preferidos da semana</h2>
<h4>Tá com fome ???</h4>

<div class="row">
    <div class="col-sm-4 col-lg-4 col-md-4">
        @foreach (var lanche in Model.LanchesPrefiridos)
        {
            @await Html.PartialAsync("_LanchesResumo", lanche);
        }
    </div>
</div>
